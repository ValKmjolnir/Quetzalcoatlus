# Quetzalcoatlus

Experimental LLM training process.

## [BBPE](bbpe/bbpe.hpp)

Byte-level Byte Pair Encoding implementation.

### Special Token

```
<|pad|>
<|unk|>
<|endoftext|>
<|im_start|>
<|im_end|>
<|unk|>
```

### Build & Usage

```bash
mkdir build && \
    cd build && \
    cmake .. -DCMAKE_BUILD_TYPE=Release && \
    make -j && \
    cd ..
```

Extract token table by:

```bash
./build/bbpe <input text>
```

Output files would be:
- `tokenizer.json`
- `tokenizer.log`

## [GPT](gpt/gpt.py)

GPT model and training process.

### Directory Structure

```
- gpt
  |- attn.py       : attention model
  |- dataloader.py : load binary data and generate batch
  |- gpt.py        : main model
  |- tokenizer.py  : read vocab/merge table generated by BBPE,
  |                  do encoding/decoding
  `- transformer.py: transformer block definition
```

### Training Process

Prepare `data/text.txt`

1. Use bbpe to generate `tokenizer.json`
2. Use `gpt/tokenizer.py` to generate `data/text.bin`
3. [WIP]
